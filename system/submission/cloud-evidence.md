# Automation Cloud evidence

No Automation Cloud run is claimed.

- CLI authentication status: `Not logged in`.
- Project debug result: blocked with `Not logged in. Run 'uip login' first.`
- Single-file debug result: blocked by the same authentication prerequisite.
- Registry pull discovered built-in extension definitions only; connectors and deployable processes require login.
- Real-Case agent, API Workflow, RPA, Action Center, and storage bindings remain unresolved and are marked `BIND REQUIRED`.

Replace this file with actual tenant/folder identifiers, process instance ID, job keys, variable evidence, incidents, Action Center outcome, and locked final-result references only after a real cloud run.
